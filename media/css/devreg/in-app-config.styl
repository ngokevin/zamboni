@import 'lib';

.devhub-form .in-app-config {
    input[type="text"] {
        width: 100%;
    }
    .is-https.hint {
        display: inline;
    }
    .preamble {
        line-height: 1.3em;
        padding-bottom: 2em;
    }
}

#in-app-private-key .secret {
    display: none;
}
